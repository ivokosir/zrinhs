foo_func = one -> (
  a = one + 2 + 3;

  aref = a;

  qwer = aref * (2 + 1) - 4;

  ref = (
    bo = false or qwer > 5;
    test = if
      bo
    then
      if
        one == 1
      then
        (
          special = one + 1;
          special * 2
        )
      else
        (
          other_special = one - 1;
          other_special * 2
        )
      end
    else
      2
    end;
    ref = bo or test > 2;
    ref = true and ref
  );
  ref
);

increment_then_twice = a -> (
  increment = a + 1;
  increment + increment
);

doubledef_a = doubledef_b = 5*5;

increment_then_twice 4
