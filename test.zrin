a = 1 + 2 + 3;

aref = a;

qwer = aref * (2 + 1) - 4;

ref = (
  bo = false or false;
  test = if bo then 3 + 2 else 2 end;
  ref = bo or test > 2;
  ref = true and ref
);

increment_then_twice = a -> (
  increment = a + 1;
  increment + increment
);

doubledef_a = doubledef_b = 5*5;

increment_then_twice 4
